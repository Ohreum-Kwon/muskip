// This file will  takes a React element root of an element tree and a container DOM element as arguments
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <App />,
  document.getElementById('root')
);
